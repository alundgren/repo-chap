# Example repository review conventions

Use fictional data in examples. Check observable behavior and concrete failure
cases. State which checks actually ran and which evidence is missing. Keep
unrelated changes out of a repair. A person makes the final merge decision.
