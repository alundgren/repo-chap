{
  "name": "repo-chap",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "repo-chap": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=esm --packages=bundle --outfile=dist/cli.js"
  },
  "devDependencies": {
    "@repo-chap/workflow": "workspace:*",
    "@repo-chap/github": "workspace:*"
  }
}
